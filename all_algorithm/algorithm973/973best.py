class Solution:
    def kClosest(self, points, K):


        #维护一个推。

        for point in points:
            #cal distance for every point to [0,0]
            #思路是什么？  维护一个堆，不断的放入。然后更新这个推。O(nlogN)复杂度。







